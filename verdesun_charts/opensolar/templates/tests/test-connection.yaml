apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opensolar.fullname" . }}-test-connection"
  labels:
    {{- include "opensolar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opensolar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
