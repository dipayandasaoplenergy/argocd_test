ingress:
  hosts:
    - host: staging.chart-example.local
      paths:
        - path: /vendor
          pathType: Prefix
          backend:
            service:
              name: vendor
              port:
                number: 80
        - path: /vendor-alt
          pathType: Prefix
          backend:
            service:
              name: vendor
              port:
                number: 8001