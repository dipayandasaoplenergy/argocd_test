apiVersion: v1
kind: Service
metadata:
  name: {{ include "vendor.fullname" . }}
  labels:
    {{- include "vendor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vendor.selectorLabels" . | nindent 4 }}
