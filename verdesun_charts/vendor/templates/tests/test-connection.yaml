apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vendor.fullname" . }}-test-connection"
  labels:
    {{- include "vendor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vendor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
